{
  "name": "fuckme",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm run build && node server.js",
    "send:daily": "node scripts/sendDaily.js",
    "test:mail": "node scripts/testEmail.js",
    "build": "npm --prefix react-ui install --no-fund --no-audit && npm --prefix react-ui run build",
    "deploy": "cd react-ui && npm run build && cd .. && pm2 restart language-app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^8.2.0",
    "@solana/web3.js": "^1.98.4",
    "dotenv": "^16.4.5",
    "ethers": "^6.15.0",
    "fastify": "^5.5.0",
    "node-fetch": "^3.3.2",
    "tencentcloud-sdk-nodejs": "^4.0.746"
  }
}
